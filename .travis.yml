language: php

sudo: false

cache:
  directories:
        - $HOME/.composer/cache

php:
  - '7.3'
  - '7.4'
  - '8.0'
  - hhvm
  - nightly

before_script:
 - travis_retry composer install --no-ansi --dev --no-interaction --no-progress --no-scripts --optimize-autoloader

script:
  - ant travis
